name = "agentic-gateway"
main = "src/index.ts"
compatibility_date = "2025-06-01"

[[kv_namespaces]]
binding = "RATE_LIMITS"
id = "REPLACE_WITH_KV_ID"
preview_id = "REPLACE_WITH_PREVIEW_KV_ID"

[vars]
BACKEND_URL = "https://your-meme-maker-url.vercel.app"
X402_PAY_TO = "REPLACE_WITH_USDC_RECEIVING_WALLET"
JOB_PRICE_CENTS = "100"

# Secrets (set via `wrangler secret put`):
# AUDIT_LOG_URL   — optional external audit sink
